<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MedicationTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/navy</item>
    </style>

    <style name="Theme.MedicationTracker" parent="Base.Theme.MedicationTracker" />

    <style name="Theme.MedicationTracker.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/navy</item> <!-- or @drawable/your_logo_bg -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>